import { Header } from "antd/lib/layout/layout"

const SubHeader =  () => {
    return (
        <Header
          className="site-layout-background"
          style={{
            padding: 0,
          }}
        ></Header>
    )
}

export default SubHeader
